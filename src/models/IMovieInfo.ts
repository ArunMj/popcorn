export interface IMovieInfo {
    title: string,
    year: string,
    runtime: number, //minutes
    genre: string[],
    actors: string[],
    plote: string[],
    language: string,
    imdbRating: number,
    imdbID: string,
    path: string
}
